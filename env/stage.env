#tell Django which settings to use
#whether it's Django/Celery/CeleryBeat/GUnicorn/runserver, they all use Django
#hence, all of them will raise error if file does not exist, or if this variable is missing from environment
#this is a lot better than setting os.environ.setdefault() at files or using --settings at commands
#no spaces between equal signs
#for env in AWS ECS, spaces and quotation marks are included in env var value
#but command in container complains that env is not set when using spaces, e.g. SOME_ENV = SOME_VAR
DJANGO_SETTINGS_MODULE=voicewake.settings.stage

LOCKDOWN_ENABLED=1
LOCKDOWN_PASSWORD=onlyforvipusers121299

SECRET_KEY=django-insecure-hdgs8@4nxkx0du^2n-gdss(!eo6i0kj6vk=gx1mddc@g=6h_^1
MAINTENANCE_MODE=0
VERSION=1.0.0

IS_EC2=1

#one postgres RDS instance can have many databases inside
#on first create, postgres is the first default db
#db identifier of the instance is not DB_NAME
#there is no way to specify DB_NAME of first default db when creating a new instance
DB_HOST=voicewakedbstage.cpe8caye8jdh.us-east-1.rds.amazonaws.com
DB_NAME=postgres
DB_USER=master
DB_PASSWORD=5RA5EgDjUhbQO5Yq5WvX
DB_PORT=5432

REDIS_ENDPOINT=clustercfg.redis-stage.ihvaze.use1.cache.amazonaws.com:6379

EMAIL_HOST_USER=noreply@voicewake.com
EMAIL_HOST_PASSWORD=rwlkrooftnfaxmno
EMAIL_PORT=465

#S3
#credentials can be omitted in EC2 that has IAM role set up
AWS_S3_ACCESS_KEY_ID=AKIA6I26BJW75QKLNZ6U
AWS_S3_SECRET_ACCESS_KEY=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
AWS_S3_REGION_NAME=us-east-1
AWS_S3_CUSTOM_DOMAIN=d3fpcqa3tewpp.cloudfront.net
AWS_S3_STATIC_BUCKET_NAME=voicewake-static-bucket
AWS_S3_MEDIA_BUCKET_NAME=voicewake-media-bucket
AWS_S3_UGC_UNPROCESSED_BUCKET_NAME=voicewake-ugc-unprocessed-bucket

#Lambda
#credentials can be omitted in EC2 that has IAM role set up
AWS_LAMBDA_ACCESS_KEY_ID=AKIA6I26BJW75QKLNZ6U
AWS_LAMBDA_SECRET_ACCESS_KEY=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
AWS_LAMBDA_REGION_NAME=us-east-1
AWS_LAMBDA_NORMALISE_FUNCTION_NAME=normalise_audio_clips

#custom AWS values
#3.81mb
AWS_S3_AUDIO_FILE_MAX_SIZE_B=4000000
#10 minutes
AWS_S3_UPLOAD_URL_EXPIRY_S=600
AWS_LAMBDA_NORMALISE_TIMEOUT_S=30

#normalise lambda environment variables
AUDIO_CLIP_PROCESSED_FILE_EXTENSION=mp3

#Vite, dev only
#need this here as source for both Vite and Django dev
VITE_PORT=5173
